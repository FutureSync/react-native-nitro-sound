///
/// HybridNativeWebSocketSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <NitroModules/Promise.hpp>
#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include <functional>

namespace margelo::nitro::sound {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NativeWebSocket`
   * Inherit this class to create instances of `HybridNativeWebSocketSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNativeWebSocket: public HybridNativeWebSocketSpec {
   * public:
   *   HybridNativeWebSocket(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNativeWebSocketSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNativeWebSocketSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNativeWebSocketSpec() override = default;

    public:
      // Properties
      virtual bool getIsConnected() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> connect(const std::string& url) = 0;
      virtual void disconnect(double code, const std::string& reason) = 0;
      virtual void sendBinary(const std::shared_ptr<ArrayBuffer>& data) = 0;
      virtual void sendText(const std::string& text) = 0;
      virtual void sendPing() = 0;
      virtual void setOnOpenListener(const std::function<void()>& callback) = 0;
      virtual void setOnTextMessageListener(const std::function<void(const std::string& /* message */)>& callback) = 0;
      virtual void setOnCloseListener(const std::function<void(double /* code */, const std::string& /* reason */)>& callback) = 0;
      virtual void setOnErrorListener(const std::function<void(const std::string& /* error */)>& callback) = 0;
      virtual void removeAllListeners() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NativeWebSocket";
  };

} // namespace margelo::nitro::sound
